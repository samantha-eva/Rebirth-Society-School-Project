<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Rebirth Society School{% endblock %}</title>

        {# Favicon #}
        <link href="data:image/x-icon;base64," rel="icon" type="image/x-icon"/>

        {# Google Fonts #}
        <link crossorigin="" href="https://fonts.gstatic.com/" rel="preconnect"/>
        <link as="style" href="https://fonts.googleapis.com/css2?display=swap&family=Manrope:wght@400;500;700;800&family=Noto+Sans:wght@400;500;700;900" onload="this.rel='stylesheet'" rel="stylesheet"/>
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet"/>

        {# TailwindCSS #}
        <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

        {# Styles personnalisés #}
        <style type="text/tailwindcss">
            :root {
                --primary-color: #f9b006;
                --secondary-color: #f0f0f0;
                --background-color: #1a1a1a;
                --text-primary: #ffffff;
                --text-secondary: #b0b0b0;
                --accent-color: #f9b006;
            }
            body {
                font-family: 'Manrope', sans-serif;
                background-color: #000;
                color: var(--text-primary);
            }
            .card-hover-effect {
                transition: transform 0.3s ease, box-shadow 0.3s ease;
            }
            .card-hover-effect:hover {
                transform: translateY(-10px);
                box-shadow: 0 20px 25px -5px rgba(249, 176, 6, 0.2), 0 10px 10px -5px rgba(249, 176, 6, 0.1);
            }
        </style>

        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% block importmap %}{{ importmap('app') }}{% endblock %}{% endblock %}
    </head>
    <body class="bg-black text-[var(--text-primary)]">
        <div class="relative flex size-full min-h-screen flex-col overflow-x-hidden">

            {# ---------------- Navbar ---------------- #}
            <header class="sticky top-0 z-50 flex items-center justify-between whitespace-nowrap border-b border-solid border-gray-800 bg-black/80 px-10 py-4 backdrop-blur-sm">
                <div class="flex items-center gap-4">
                    <div class="text-[var(--primary-color)]">
                    <img src="{{ asset('images/logo.png') }}" style="width: 100px; height: auto;" />
                    </div>
                    <h2 class="text-xl font-bold leading-tight tracking-[-0.015em]">Rebirth Society School</h2>
                </div>
                <nav class="hidden md:flex items-center gap-9 text-lg font-medium leading-normal">
                        <a href="{{ path('app_home') }}"
                        class="text-gray-300 hover:text-[var(--primary-color)] transition-colors duration-300">
                            Home
                        </a>
                        <a href="{{ path('app_shop') }}"
                        class="text-gray-300 hover:text-[var(--primary-color)] transition-colors duration-300">
                            Shop
                        </a>
                        <a href="#"
                        class="text-gray-300 hover:text-[var(--primary-color)] transition-colors duration-300">
                            Packs Vidéo
                        </a>
                        <a href="{{ path('app_about') }}"
                        class="text-gray-300 hover:text-[var(--primary-color)] transition-colors duration-300">
                            About
                        </a>
                    </nav>

                <div class="flex items-center gap-4">
                    <button class="flex items-center justify-center rounded-full p-2 hover:bg-gray-800 transition-colors duration-300">
                        <span class="material-symbols-outlined text-gray-300 hover:text-[var(--primary-color)]">shopping_cart</span>
                    </button>
                    <button class="bg-[var(--primary-color)] text-black font-semibold py-2 px-6 rounded-full hover:bg-yellow-500 transition-colors duration-300">
                        <a href="{{ path('app_login') }}">Connexion</a>
                    </button>
                </div>
            </header>
            {# ------------- /Navbar ---------------- #}

            {# Contenu spécifique de la page #}
            {% block body %}{% endblock %}

        </div>
    </body>
</html>
